///
// Анимация 
// || анимация движения и смерти персонажа 
// || анимация финишного круга 
// || анимация переходов в меню

// Музыка
// || добавление музыки, звуков

// Меню 
// || добавление меню выборки уровня
// || добавление меню паузы 		if(Input.GetButtonUp("P")){
  					                if(!paused){ // 
  					                Time.timeScale = 0;
  					                paused=true;
					                }
// || добавление меню выбора цвета // Enum
// || добавление раздела просмотра достижений

// Гейм контроллер
// || добавление файла рекордов
// || добавление прогресс слайда для каждого уровня 

// Механика, геймплей ✓
// || добавление самолета и игровой механики полета
// 	|| Когда куб попадает в триггер смены режима геймплея, 
//	|| смена player из куба на самолет(модель нарисовать)
//	|| В обратную сторону такой же телепорт		
///
